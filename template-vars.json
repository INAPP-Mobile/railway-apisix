{
  "ETCD_HOST": {
    "description": "etcd sibling service private hostname (auto-resolved)",
    "default": "${{etcd.RAILWAY_PRIVATE_DOMAIN}}"
  },
  "APISIX_ADMIN_KEY": {
    "description": "Admin API key for dashboard and admin API (auto-generated)",
    "default": "${{secret(32)}}"
  },
  "PORT": {
    "description": "Port APISIX listens on (Railway injects this)",
    "default": "9080"
  }
}