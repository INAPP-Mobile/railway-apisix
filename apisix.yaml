routes:
  - uri: /health
    plugins:
      response-rewrite:
        status_code: 200
        body: "ok"
  - uri: /*
    plugins:
      response-rewrite:
        status_code: 200
        body: "ok"
#END