routes:
  - uri: /health
    plugins:
      response-rewrite:
        status_code: 200
        body: "ok"
  - uri: /get
    upstream:
      type: roundrobin
      nodes:
        "httpbin.org:80": 1
#END
